// This file overrides the jadb dependency to use a stable version
configurations.all {
    resolutionStrategy.dependencySubstitution {
        substitute module('com.github.vidstige:jadb:master-SNAPSHOT') with module('com.github.vidstige:jadb:v1.2.1')
    }
}